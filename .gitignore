# Secrets
.env
.env.local

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
env/
*.egg-info/
build/
dist/

# Node
node_modules/
.vite/

# Pre-pivot engine directory (deleted from git in commit 99df6e0; if a
# locked empty dir lingers on Windows, ignore it here so future
# `git status` is clean. Manual filesystem cleanup recommended once
# whatever has the dir handle releases.)
engine/

# Internal sprint planning docs (S2 / S3 engine-era, S4 skill-pivot
# planning + status updates). Kept locally for the developer's own
# reference and AI-assistant context, but not part of the public
# deliverable. The course-facing architecture rationale lives in
# docs/project_plan.md / .zh.md, which are tracked.
docs/plan/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Claude Code per-project workflow config — sub-agents, slash commands,
# hooks, per-user settings. Useful for the developer + AI assistant
# working in this repo, but not part of the public deliverable. Same
# rationale as docs/plan/.
.claude/
