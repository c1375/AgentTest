"""AgentTest eval harness.

Samples and ground-truth metadata under `samples/`; deterministic risk
injections under `injections/`; the runner that produces recall and
precision numbers under `runner.py` (added in S2 Step 7).
"""
